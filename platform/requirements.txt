fastapi>=0.111.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pdfplumber>=0.11.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# Moss — in-process semantic retrieval for recoupment recall.
# Requires Python >= 3.10; on 3.9 the marker skips it and
# agents/semantic_matcher.py self-disables to the regex-only path.
moss>=1.12.0; python_version >= "3.10"
